#include "main.h"
/**
 * print_sign - lower case
 *
 * @c: ASCII code
 *
 * Return: Always 0 (Success)
 */

int print_sign(int n)
{
	if ((n > 0 )
	{
		putchar(43);
		return (1);
	}
	else
	{
		putchar(45);
		return (0);
	}
}
